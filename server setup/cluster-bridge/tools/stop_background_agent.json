{
  "name": "stop_background_agent",
  "description": "Gracefully halt an active background agent by its ID.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent_id": {
        "type": "string",
        "description": "The ID of the agent to halt (e.g. AGENT-A1B2C3)."
      }
    },
    "required": [
      "agent_id"
    ]
  }
}